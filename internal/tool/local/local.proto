syntax = "proto3";

option go_package = "github.com/kanengo/akasar/internal/tool/local";

package single;

import "runtime/protos/config.proto";

message LocalConfig {
  runtime.AppConfig app = 1;

  message ListenerOptions {
    string address = 1;
  }

  map<string, ListenerOptions> listeners = 2;
}